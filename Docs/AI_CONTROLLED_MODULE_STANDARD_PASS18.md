# Titan AI-Controlled Module Standard PASS18

Every module has one sidebar link and one assembled control panel page. The UI folder is a component library; each module must build a ControlPanel from those elements.

User-facing AI is always **Zero**. Internal module agents are hidden specialists.

Required layout: KPI widgets, Zero Operator Card, shortcuts, settings, and one Unified Tabbed Table Card for all tables.
